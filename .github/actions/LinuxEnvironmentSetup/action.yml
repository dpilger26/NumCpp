name: Linux Environment Setup
description: "Sets up the linux environment"

runs:
  using: "composite"
  steps:
    - name: Setup conda
      uses: conda-incubator/setup-miniconda@v3
      with:
        miniconda-version: Miniconda3-py310_23.10.0-1-Linux-x86_64
        activate-environment: test
        environment-file: test/pytest/environment.yml
        python-version: 3.10

    - name: Activate Env
      shell: bash -l {0}
      run: |
        conda activate test 

    - name: Install Dependency libs
      shell: bash -l {0}
      run: |
        sudo apt update
        sudo apt install -y lsb-release wget software-properties-common gnupg libboost-all-dev
        wget https://apt.llvm.org/llvm.sh
        chmod u+x llvm.sh
        sed -i 's/add-apt-repository "${REPO_NAME}"/add-apt-repository -y "${REPO_NAME}"/g' llvm.sh 
        sudo ./llvm.sh 16
        sudo ln -sfn /usr/bin/clang++-16 /usr/bin/clang++
